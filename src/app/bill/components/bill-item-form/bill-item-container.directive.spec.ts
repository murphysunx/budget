import { BillItemContainerDirective } from './bill-item-container.directive';

describe('BillItemContainerDirective', () => {
  it('should create an instance', () => {
    const directive = new BillItemContainerDirective();
    expect(directive).toBeTruthy();
  });
});
